if (window.console && window.console.log) {
  console.log('Edit file main.js to see livereload in action');
  console.log('Current time is', Date.now());
  console.log('why are we displaying the current time?');
  console.log('So that you can see that CSS is hot deployed without full reload of the page!');
  console.log('(which is awesome)');
}
